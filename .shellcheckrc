# Скрипты подключают общую библиотеку через переменный путь:
#   source "$SCRIPT_DIR/../scripts/lib.sh"
# shellcheck не может статически разрешить путь и выдаёт SC1091 (info) —
# это ложное срабатывание, а не дефект. Глушим репозиторно.
disable=SC1091
